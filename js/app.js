'use strict';

angular.module("dashboard", ['dashboard.controllers', 'dashboard.factories', 'dashboard.directives','dashboard.services', 'dashboard.constants', 'ui.router']);

angular.module("dashboard.controllers", []);

angular.module("dashboard.factories", []);

angular.module("dashboard.directives", []);

angular.module("dashboard.services", []);

angular.module("dashboard.constants", []);
